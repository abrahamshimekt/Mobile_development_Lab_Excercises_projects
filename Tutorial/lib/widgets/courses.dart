import 'package:flutter/material.dart';
class PythonCourse extends StatelessWidget {
  const PythonCourse();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Java extends StatelessWidget {
  const Java();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class javaScript extends StatelessWidget {
  const javaScript();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Web extends StatelessWidget {
  const Web();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Mobile extends StatelessWidget {
  const Mobile();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Linux extends StatelessWidget {
  const Linux();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Flutter extends StatelessWidget {
  const Flutter();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Css extends StatelessWidget {
  const Css();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}
class Djnago extends StatelessWidget {
  const Djnago();

  @override
  Widget build(BuildContext) {
    return Scaffold(appBar: AppBar(),
        body: Container(alignment: Alignment.center,
            padding: EdgeInsets.all(10),
            child: Column(
              children: [Image.asset("assets/images/img_3.png")],)));
  }
}